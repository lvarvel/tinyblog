.skip-one.three.twelfths
  = form_tag [:admin, :login] do
    .field
      = label_tag :user_email
      = text_field_tag :user_email, params[:user_email]
    .field
      = label_tag :user_password
      = password_field_tag :user_password

    .actions
      = submit_tag "Log In"